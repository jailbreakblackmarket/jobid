{
  "name": "server-selector",
  "version": "1.0.0",
  "description": "Cloudflare Worker that selects and returns recent servers",
  "main": "index.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.74.0"
  }
}
